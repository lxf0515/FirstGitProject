<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.took.firstgit">

    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".GApp"
        >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ui.activity.PagerBottomTabStripActivity"
            >
        </activity>

        <activity
            android:name=".ui.activity.BottomTabActivity"
            >

        </activity>

        <activity
            android:name=".ui.activity.lock.LockViewActivity"
            >
        </activity>

        <activity
            android:name=".ui.activity.lock.MyLockViewActivity"
            >
        </activity>

        <activity
            android:name=".ui.tab.SlidingTabStripActivity"
            >
        </activity>
        <activity
            android:name=".ui.password.PassWordActivity"
            >
        </activity>

        <activity
            android:name=".ui.picture.PictureSelectActivity"
            >
        </activity>

        <activity
            android:name="com.lzy.imagepicker.ui.ImagePreviewDelActivity"
            android:theme="@style/ImagePickerThemeFullScreen"/>

        <activity android:name=".ui.main.MainActivity" />

        <activity android:name=".ui.guide.GuideActivity" />

        <activity android:name=".ui.activity.BottomSlidingTabStripActivity" />

        <activity android:name=".ui.auto.AutoViewPagerCardActivity" />

        <activity android:name=".ui.gallery.GalleryActivity" />

        <activity android:name=".ui.Menu.FloatingActionMenuActivity" />
    </application>

</manifest>